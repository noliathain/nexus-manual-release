# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# The semantic index files (semantic_index.npz) ARE committed
# — they ship pre-built so the repo runs offline out of the
# box. Only the legacy encoder-named cache files are ignored.
artifacts/products/*/graph/semantic_index_minishlab*.npz
artifacts/products/*/graph/semantic_index_potion*.npz

# Editor / IDE
.vscode/
.idea/
.DS_Store
*.swp

# Logs / temp
*.log
.env
